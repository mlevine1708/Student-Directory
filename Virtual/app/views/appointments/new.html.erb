<h1>Make an appointment for <%= @appointment.teacher.name %></h1>

<%= form_for [@teacher, @appointment] do |f|%>
  <%= f.hidden_field :teacher_id%>
  <div>
    <%= f.label :name%>
    <%= f.text_area :name%>
  </div>

  <div>
    <%= f.label :date, "Type Date"%>
    <%= f.datetime :date%>
  </div>
  <br>
  <%= f.submit %>
<% end %>

# rework form, is the appointment for a student or a teacher?
# do students belong to teachers?
#story - why it's working the way it's working
#coherent and works
#student directory system?
